# Local development
VITE_API_URL=http://localhost:8080
VITE_SIGNER_URL=http://localhost:8081

# GitHub Pages deployment:
# Set VITE_API_URL as a GitHub repository Actions variable to your public HTTPS API.
# VITE_SIGNER_URL is optional and should only be exposed for the devnet import flow.
